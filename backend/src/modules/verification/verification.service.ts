// This service is reserved for higher-level verification workflows.
// For MVP, the worker directly uses OCR / parser / validation / scoring services.

export const verificationService = {
  // Placeholder for future orchestration methods.
};
