# DocuTrust Flutter App

A minimal Flutter app for uploading documents and viewing verification results.

## Run

```bash
cd flutter_app
flutter pub get
flutter run
```

The app expects a backend API running at `http://localhost:3000/api`.
